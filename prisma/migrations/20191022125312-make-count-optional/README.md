# Migration `20191022125312-make-count-optional`

This migration has been generated by Jonathan Van Buren at 10/22/2019, 12:53:12 PM.
You can check out the [state of the datamodel](./datamodel.prisma) after the migration.

## Database Steps

```sql
ALTER TABLE "sas"."Item" DROP COLUMN "count",
ADD COLUMN "count" integer   ;
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration 20191022125150-add-cosnumable..20191022125312-make-count-optional
--- datamodel.dml
+++ datamodel.dml
@@ -21,7 +21,7 @@
   id String @default(cuid()) @id
   name String @unique
   description String
   category Category
-  count Int
+  count Int?
   consumable Boolean @default(false)
 }
```

## Photon Usage

You can use a specific Photon built for this migration (20191022125312-make-count-optional)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/20191022125312-make-count-optional'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```
